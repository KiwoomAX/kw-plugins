# kw-plugins
##  실제 플러그인이 설치되는 환경은 WINDOW and Powershell
Window 안에서 Powershell (5 or 7) 로 클로드 코드 (혹은 다른 코딩 하네스)를 사용한다는 전제가 필요하다. 따라서 커맨드나 문법이 그에 따라 맞춰져야 한다.





## 관리 설정(managed settings)을 다시 논하기 전에

검토했고 기각했다. 전달 수단이 넷이라는 것, `HKCU` 경로가 왜 컨트롤 타워의 `CLAUDE.md`
블록보다 오히려 약한지, 서버 경로가 왜 스스로 부트스트랩하지 못하는지가
`docs/superpowers/specs/2026-09-06-control-tower-design.md` 의 「왜 관리 설정이 아닌가」에
적혀 있다. 같은 결론을 다시 도출하기 전에 그 절을 먼저 읽어라.
